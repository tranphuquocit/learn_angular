export interface ProductModel {
  id?: number,
  type?: string,
  description?: string,
  image?: string,
  price?: string,
  quantity?: number,
  like?: number,
  sold?: number,
  isLiked?: boolean
}
